# No Visual Change Assertion

The planned baseline branch will contain the exact same bytes as the supplied Build. It will not change HTML, CSS, JavaScript, images, text, layout, fonts, animation, routes, or runtime behavior.
